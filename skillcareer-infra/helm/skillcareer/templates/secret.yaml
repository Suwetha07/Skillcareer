{{- if and .Values.secrets .Values.secrets.create }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.secrets.name | default "skillcareer-secret" }}
type: Opaque
data:
{{- range $key, $value := .Values.secrets.data }}
  {{ $key }}: {{ $value | toString | b64enc }}
{{- end }}
{{- end }}

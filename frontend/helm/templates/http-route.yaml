apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ .Chart.Name }}
spec:
  rules:
    - backendRefs:
        - name: {{ .Chart.Name }}
          port: {{ .Values.service.port }}
